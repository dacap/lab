# En cmake podemos especificar que queramos compilar un archivo
# ejecutable (llamado hola-mundo.exe en este caso) que se origina al
# compilar una lista de archivos de código fuente (en nuestro caso
# sólo uno, hola-mundo.cpp)
add_executable(hola-mundo hola-mundo.cpp)
